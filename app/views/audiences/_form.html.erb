<%= form_for(@audience) do |f| %>
  <% if @audience.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@audience.errors.count, "error") %> prohibited this audience from being saved:</h2>

      <ul>
      <% @audience.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_name %><br>
    <%= f.text_field :user_name %>
  </div>
  <div class="field">
    <%= f.label :user_email %><br>
    <%= f.text_field :user_email %>
  </div>
  <div class="field">
    <%= f.label :user_avatar_url %><br>
    <%= f.text_field :user_avatar_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
